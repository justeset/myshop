# 🛒 Store Project

**Store Project** — это интернет-магазин, реализованный на **Django 4+** и **Django REST Framework**, с полностью функциональным REST API для управления пользователями, товарами, корзиной и заказами. Проект полностью контейнеризован через Docker и поддерживает JWT-аутентификацию.

---

## 📌 Основной функционал

### Пользователи
- Регистрация и авторизация через JWT
- Профиль пользователя
- Личный баланс (можно пополнять)

### Корзина
- Добавление товара
- Удаление товара
- Изменение количества
- Просмотр текущей корзины

### Товары
- Название, описание, цена, количество на складе
- Только админ может создавать/редактировать/удалять
- Все пользователи могут просматривать список товаров (`GET /api/catalog/products/`)

### Заказы
- Создание заказа из корзины
- Проверка наличия товара и баланса пользователя
- Списание баланса и количества со склада
- Очистка корзины после заказа
- Логирование успешного заказа

---

## 🛠 Стек технологий

| Компонент                  | Используется                                   |
|-----------------------------|-----------------------------------------------|
| Язык                        | Python 3.11+                                  |
| Фреймворк                   | Django 4+                                     |
| ORM                         | Django ORM (PostgreSQL)                       |
| API                         | Django REST Framework                         |
| Авторизация                 | JWT                                           |
| Документация API            | drf-spectacular                               |
| Email/log                   | logging / Django email backend                |
| Контейнеризация             | Docker + Docker Compose                        |
| Тесты                       | unittest или pytest                            |

---

## 🚀 Запуск проекта через Docker

1. Склонируй репозиторий:

```bash
git clone git@github.com:justeset/myshop.git
cd myshop
```

2. Создай файл `.env` в корне проекта с содержимым:

```env
DJANGO_SECRET_KEY=your_secret_key
DJANGO_DEBUG=True

POSTGRES_DB=shop_db
POSTGRES_USER=shop_user
POSTGRES_PASSWORD=shop_password
POSTGRES_HOST=db
POSTGRES_PORT=5432

SIMPLE_JWT_ACCESS_TOKEN_LIFETIME=30m
SIMPLE_JWT_REFRESH_TOKEN_LIFETIME=1d

```

3. Собери и запусти контейнеры:

```bash
docker-compose up --build
```

4. Приложение будет доступно по адресу:

```
http://127.0.0.1:8000/
```

Swagger-документация API:

```
http://127.0.0.1:8000/api/docs/
```

---

## 🧪 Тесты

Запустить тесты Django:

```bash
docker-compose exec web python manage.py test
```

или локально:

```bash
python manage.py test
```

---

## 🔑 Примечания

- Все секреты и пароли хранятся в `.env` и **не коммитятся** в репозиторий.  
- JWT-токены используются для безопасной авторизации API.  
- Админка доступна по:

```
http://127.0.0.1:8000/admin/
```

- Docker обеспечивает одинаковое окружение для всех разработчиков.



